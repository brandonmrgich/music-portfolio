const { AUDIO_WIP, AUDIO_REEL, AUDIO_SCORING } = require("../config/config");

const AudioTypes = {
    "AUDIO_WIP": AUDIO_WIP,
    "AUDIO_REEL": AUDIO_REEL,
    "AUDIO_SCORING": AUDIO_SCORING,
};

module.exports = {
    AudioTypes,
};
